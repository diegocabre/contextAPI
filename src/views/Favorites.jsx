import { MyProvider } from "../context/MyProvider";

const Favorites = () => {
  return (
    <div>
      <h1>Favorites</h1>
      <MyProvider></MyProvider>
    </div>
  );
};

export default Favorites;
